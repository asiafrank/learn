package coursex;

/**
 * 快排
 */
public class C03PartitionQuickSort {

    //
    // TODO: 利用 partition 求第 k 大的数

    /**
     * TODO：荷兰国旗问题
     * 将 arr[l..r] 分成三个部分：
     * 小于 arr[r] 的
     * 等于 arr[r] 的
     * 大于 arr[r] 的
     * 返回等于 arr[r] 的左边界和右边界。
     */
    public static int[] netherLandsFlag(int[] arr, int l, int r) {
        return new int[]{};
    }
}
