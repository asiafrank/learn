#include <iostream>

#include "example.h"
// #include "lib/Sorts.h"
// #include "lib/Printer.h"

// VSCode 的 CMakeTools 插件 DEBUG 按钮不起作用。直接用 F5 按钮启动 DEBUG 程序

int main() {
    // example10_2::start();
    example10_5::start();
    return 0;
}